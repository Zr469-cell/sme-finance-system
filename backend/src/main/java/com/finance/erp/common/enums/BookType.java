package com.finance.erp.common.enums;

public enum BookType {
    PERSONAL,           // 个人/家庭 (仅核心记账)
    BUSINESS_SIMPLE,    // 商贸/店铺 (进销存 + 记账)
    MANUFACTURING,      // 工厂/制造 (BOM + 生产 + 进销存 + 记账)
    NON_PROFIT          // 非营利组织
}